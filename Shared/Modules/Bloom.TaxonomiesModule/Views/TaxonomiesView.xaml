<UserControl x:Class="Bloom.TaxonomiesModule.Views.TaxonomiesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    
    <Grid>
         <StackPanel Orientation="Vertical">
            <telerik:RadButton Command="{Binding Path=AddLibraryTabCommand}">Library</telerik:RadButton>
            <telerik:RadButton Command="{Binding Path=AddPersonTabCommand}">Person</telerik:RadButton>
            <telerik:RadButton Command="{Binding Path=AddArtistTabCommand}">Artist</telerik:RadButton>
            <telerik:RadButton Command="{Binding Path=AddAlbumTabCommand}">Album</telerik:RadButton>
            <telerik:RadButton Command="{Binding Path=AddSongTabCommand}">Song</telerik:RadButton>
            <telerik:RadButton Command="{Binding Path=AddPlaylistTabCommand}">Playlist</telerik:RadButton>
         </StackPanel>
    </Grid>
    
</UserControl>
