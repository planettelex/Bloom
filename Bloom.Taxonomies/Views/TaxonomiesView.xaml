<UserControl x:Class="Bloom.TaxonomiesModule.Views.TaxonomiesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    
    <Grid>
         <StackPanel Orientation="Vertical">
            <Button Command="{Binding Path=AddLibraryTabCommand}">Library</Button>
            <Button Command="{Binding Path=AddPersonTabCommand}">Person</Button>
            <Button Command="{Binding Path=AddArtistTabCommand}">Artist</Button>
            <Button Command="{Binding Path=AddAlbumTabCommand}">Album</Button>
            <Button Command="{Binding Path=AddSongTabCommand}">Song</Button>
            <Button Command="{Binding Path=AddPlaylistTabCommand}">Playlist</Button>
         </StackPanel>
    </Grid>
    
</UserControl>
